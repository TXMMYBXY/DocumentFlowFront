<Window x:Class="DocumentFlowing.Views.Admin.UpdateUserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:common="clr-namespace:DocumentFlowing.Common"
        mc:Ignorable="d"
        Title="UpdateUserView" Height="330" Width="600"
        ResizeMode="NoResize">
    <Window.Resources>
        <common:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Email:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" 
                 Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="2" Text="ФИО:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" 
                 Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="4" Text="Отдел:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="5"
                  IsEditable="True"
                  ItemsSource="{Binding DepartmentSuggestions}"
                  Text="{Binding Department, UpdateSourceTrigger=PropertyChanged}"
                  Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="6" Text="Роль:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="7"
                  ItemsSource="{Binding Roles}"
                  DisplayMemberPath="Title"
                  SelectedItem="{Binding SelectedRole}"
                  Margin="0,0,0,10"/>
        
        
        <TextBlock Grid.Row="8"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   Margin="0,0,0,10"
                   TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, 
                   Converter={StaticResource StringToVisibilityConverter}}"/>
        
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" 
                    Command="{Binding SaveCommand}"
                    Width="80"
                    Margin="0,0,10,0"
                    IsDefault="True"/>
            
            <Button Content="Отмена" 
                    Command="{Binding CancelCommand}"
                    Width="80"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
